我们已经知道：

- 浮点数的本质，是二进制科学计数法；
- 一个数可以拆成：
    - 符号（Sign）
    - 有效数字（Significand）
    - 指数（Exponent）

接下来真正的问题是：

> 这些东西到底怎么塞进计算机里？

这就是 IEEE 754 要解决的事情。

---

## IEEE 754 的结构

以最常见的 **32 位单精度（float）** 为例：

```text
┌──────────┬──────────────┬─────────────────────────┐
│  sign    │   exponent   │       fraction          │
│  符号位  │    指数位     │        尾数位            │
├──────────┼──────────────┼─────────────────────────┤
│  1 bit   │    8 bit     │        23 bit           │
└──────────┴──────────────┴─────────────────────────┘
```

共 1 + 8 + 23 = 32 bit。

- **符号位（sign）**：`0` 正 `1` 负。直白，一句话的事。
- **指数位（exponent）**：决定小数点飞多远、往哪飞。8 个比特。
- **尾数位（fraction）**：存主干数字的小数部分。23 个比特。

符号位很直白，但指数位和尾数位——IEEE 754 在上面施了两个精巧的魔术。我们先从指数位开始，因为它决定了尺子的“量程”。

---

### 魔术一：Bias（偏移量）——让比较飞起来

指数有正有负——`-10`、`-3`、`0`、`5`、`20`。

如果 8 bit 直接存有符号数，比较两个浮点数谁大谁小时，硬件得先看符号位、再分路径处理，电路会变复杂。

IEEE 754 的解法很巧妙：**给所有指数统一加上一个偏移量，把负数全部抬成正数。**

单精度的偏移量是 **127**。规则很简单：

```text
存储值 = 实际指数 + 127

实际指数 -3  →  存储 124
实际指数  0  →  存储 127
实际指数  5  →  存储 132
```

加了偏移之后，指数位永远是一个无符号整数。比较两个浮点数大小退化成了无脑操作：

> **谁的无符号整数大，谁的浮点数就大。**

一条电路，不做分支判断。这个偏移量就叫 **Bias**。

**那为什么偏偏是 127？**

这就得看 8 位指数一共 256 种状态（0～255）是怎么分的了。IEEE 754 把两头各留了一个特殊值：

| 存储值        | 用途             |
| :--------- | :------------- |
| `0`（全 0）   | 零 & 次正规数       |
| `255`（全 1） | 无穷大 & NaN      |
| `1 ~ 254`  | **正规数**（正常使用的） |

正规数只剩 254 个状态。我们当然希望正指数和负指数大致各一半——254 ÷ 2 = 127。所以偏移量的目标就是**让存储值 127 对应实际指数 0**，两边各分约 127 个：

```text
存储值 1    →  1 − 127 = −126（实际指数）    ← 最小正规指数
存储值 127  →  127 − 127 = 0  （实际指数）   ← 零点
存储值 254  →  254 − 127 = 127 （实际指数）  ← 最大正规指数
```

实际指数范围 `[−126, 127]`。

统一到任意位宽 k：

```text
bias = 2^(k−1) − 1

k = 8   →  bias = 127   （单精度）
k = 11  →  bias = 1023  （双精度）
```

这就是指数位在 IEEE 754 里的存储方式：存的时候加 bias，读的时候减回去。简单，但极精巧。

---

### 魔术二：隐藏的 1（Hidden Bit）——白嫖一位精度

回忆规范化二进制科学计数法：

```text
1.0101 × 2⁴
1.1110 × 2⁻²
1.0010 × 2⁸
```

会发现一个规律：

> 整数部分永远是 1。

因为如果前面是 0：

```text
0.101 × 2⁴
```

继续左移就能得到：

```text
1.01 × 2³
```

既然规范化之后最高位一定是 1：

```text
1.xxxxxxxxx
```

那这个 1 还有必要存吗？

IEEE 754 的回答：

> 没必要。

直接省掉。只把小数点后面的 `xxx` 存进尾数位，读取时自动把前面的 `1.` 补回来。

```text
实际存储：    0 1 0 0 0 1   （23 bit）
读取时补回： 1.0 1 0 0 0 1  （等效 24 bit 精度）
```

23 个比特，白赚一位，等效 24 位精度。

这就是著名的：

> Hidden Bit（隐含位）

或者：

> Implicit Leading One（隐式最高位）

至此，我们可以得出一个正规数的解码公式：

> **一个正规数的值 = `(−1)^sign × (1.fraction) × 2^(exponent − bias)`**

---

## 实战：亲手把 20.25 塞进 IEEE 754

理论说完了，我们来编码一次。把 `20.25₁₀` 从头到尾存成一个 32 位单精度浮点数。

### 第一步：拆成二进制

**整数部分** `20₁₀` ——除 2 取余：

```text
20 ÷ 2 = 10 ... 0
10 ÷ 2 =  5 ... 0
 5 ÷ 2 =  2 ... 1
 2 ÷ 2 =  1 ... 0
 1 ÷ 2 =  0 ... 1
```

从下往上读：**`10100₂`**

**小数部分** `0.25₁₀` ——乘 2 取整：

```text
0.25 × 2 = 0.5 ... 0
0.5  × 2 = 1.0 ... 1
```

从上往下读：**`01₂`**

合并：**`20.25₁₀ = 10100.01₂`**

### 第二步：规范化

小数点左移 4 位：

```text
10100.01₂ = 1.010001₂ × 2⁴

尾数：1.010001₂
实际指数：4
```

### 第三步：符号位

正数 → **`sign = 0`**

### 第四步：指数位（加 Bias）

实际指数 4，加上偏移量 127：

```text
4 + 127 = 131
131₁₀ = 10000011₂
```

指数位：**`10000011`**

### 第五步：尾数位（去隐含 1）

规范化尾数 `1.010001`，去掉隐含的最高位 `1`，只留小数部分，补零到 23 位：

```text
1.010001  →  01000100000000000000000
```

### 组装

三块拼在一起：

```text
sign:  0
exp:   10000011
frac:  01000100000000000000000

0 10000011 01000100000000000000000
```

这就是 `20.25₁₀` 在内存里的真实样子——32 个比特，不多不少。

### 反向验证

从 `0 10000011 01000100000000000000000` 倒推：

1. 符号位 `0` → 正数 ✓
2. 指数位 `10000011₂` = 131 → 131 − 127 = 4 ✓
3. 尾数位补回隐含 `1` → `1.010001₂`
4. `1.010001₂ × 2⁴ = 10100.01₂ = 20.25₁₀` ✓

完美还原。你已经看到了浮点数在内存里的真实面貌。

---

## 指数位的禁区：特殊值全解

前面提过，指数位 `0` 和 `255` 被保留，它们承担着特殊使命。下面这张表总结了全部位模式：

| 指数位     | 尾数位 | 表示的值 |
| :------ | :-- | :--- |
| 全 0     | 全 0 | ±0   |
| 全 0     | 非 0 | 次正规数 |
| 1 ~ 254 | 任意  | 正规数  |
| 全 1     | 全 0 | ±∞   |
| 全 1     | 非 0 | NaN  |

---

### ±0

```text
0 00000000 00000000000000000000000  →  +0
1 00000000 00000000000000000000000  →  −0
```

数学里没有正零负零。IEEE 754 有——因为它需要保留极限方向的信息：

```javascript
1 / +0    // Infinity
1 / -0    // -Infinity
```

---

### 次正规数——渐进下溢的艺术

正规数最小指数 −126，对应 `1.0 × 2⁻¹²⁶ ≈ 1.18 × 10⁻³⁸`。那它和 0 之间怎么办？

IEEE 754 不希望突然断层，于是设计了次正规数。当指数位全 0 时，规则变了：

- 隐含位不再是 `1`，而变成 `0`
- **实际指数强制使用最小正规数的指数值 `-126`**

于是，我们从 `0` 到 `1.0 × 2⁻¹²⁶` 之间创造了一个平滑的过渡带，其解码公式变为：

> **一个次正规数的值 = `(−1)^sign × (0.fraction) × 2^(−126)`**

正规数的 `1.xxx × 2⁻¹²⁶` 向下无缝衔接到 `0.xxx × 2⁻¹²⁶`，数字能够继续向 0 靠近。最小正数约为 `1.4 × 10⁻⁴⁵`。

这种设计被称为：

> Gradual Underflow（渐进下溢）

---

### ±Infinity

```text
0 11111111 00000000000000000000000  →  +Infinity
1 11111111 00000000000000000000000  →  −Infinity
```

```javascript
1 / 0     // Infinity
-1 / 0    // -Infinity
```

IEEE 754 认为：

> 与其让程序直接崩溃，不如把结果映射到无穷大。

---

### NaN

```text
0 11111111 001001...  →  NaN（Not a Number）
```

```javascript
0 / 0              // NaN
Infinity / Infinity // NaN
```

最著名的性质：

```javascript
NaN === NaN  // false
```

因为：

> NaN 不等于任何东西，包括它自己。

---

## 真正的噩梦：0.1 为什么存不准

绝大多数程序员第一次接触 IEEE 754，都是因为这行代码：

```javascript
0.1 + 0.2  // 0.30000000000000004
```

为什么？因为 `0.1 = 1/10` 在二进制里无法有限表示——就像 `1/3 = 0.333333...` 在十进制里永远写不完一样。

把 0.1 转成二进制：

```text
0.0001100110011001100110011...
        0011 0011 0011 ...   ← 无限循环
```

但 float 只有 23 bit 尾数，于是只能截断 + 舍入。最终存进去的不是 `0.1`，而是 `0.100000001490116119...` 附近的某个值。

0.2 也是如此。所以真正发生的是：

```text
(0.1 的近似值) + (0.2 的近似值) = 0.30000000000000004 ≠ 0.3
```

这里有一个非常重要的结论：

> 浮点数不是精确存储实数。
>
> 浮点数是在存储最接近的可表示值。

---

## 舍入规则

既然很多数无法精确表示，就必须决定：

> 到底舍入到哪一个可表示值？

IEEE 754 默认采用：

> Round to Nearest, Ties to Even
>
> 向最近值舍入；如果正好在中间，选偶数。

---

### 最近值

假设可表示值只有 `1.2` 和 `1.3`：

- `1.24` 更接近 `1.2`，舍入到 `1.2`
- `1.27` 更接近 `1.3`，舍入到 `1.3`

### 中点问题

如果结果是 `1.25`，它距离 `1.2` 和 `1.3` 完全一样。这叫：

> Tie（平局）

### 为什么选偶数

如果规定永远向上，长期统计后整体偏大；永远向下，整体偏小。于是 IEEE 754 选择：

> 哪边最低有效位是偶数，就选哪边。

这样长期来看向上 ≈ 向下，误差更容易互相抵消。

```text
1.25 → 1.2  （2 是偶数）
1.35 → 1.4  （4 是偶数）
```

这就是 Banker's Rounding（银行家舍入），也是现代计算机最常见的舍入方式。

---
## 用 6 位浮点数，亲手摸透 IEEE 754 的精度底牌

**浮点数的本质，是用有限个离散刻度去逼近连续的实数轴。**
我们经常听到“浮点数有误差”“不要直接比较相等”，但那误差到底长什么样？
与其背诵规则，不如动手设计一个极简版的浮点数——**6 位浮点数（my_float）**。一旦你用纸笔跑完它的全部“格子”，IEEE 754 的精度模型就再也不会神秘。

### 1. 我们的迷你浮点数：my_float

我们只给 6 个二进制位，结构如下：

- **1 bit 符号位**
- **3 bit 指数位**（偏移量 Bias = 3）
- **2 bit 尾数位**（隐藏的整数位固定为 `1`）

3 位指数一共有 8 种状态（000～111），IEEE 754 传统上会用两头做特殊值：

- **指数位全 0**：预留给 0 和次正规数
- **指数位全 1**：预留给 ±∞ 和 NaN

因此，正规数的指数存储值只能是 1～6。偏移量选 3，是为了让正负指数大致对称：

- 存储值 1 → 实际指数 \(1-3 = -2\)
- 存储值 3 → 实际指数 0
- 存储值 6 → 实际指数 \(6-3 = 3\)

于是我们正规数的实际指数范围是 **`[-2, 3]`**，共 6 档。

2个尾数位能组合出 `00, 01, 10, 11` 四种，加上隐藏的 `1`，有效数字就是：
- `1.00`、`1.01`、`1.10`、`1.11`

所以**每个指数档位里，都有4个可以精确表示的数**。这些数之间的间隔，就是著名的 **ULP**（Unit in the Last Place，单位最小精度）。

下面，我们就从最小指数到最大指数，一档一档地看过去。

---

### 2. 漫游全部6个指数档位

#### 指数 = -2（存储值 1）

真实指数 -2，意味着我们把尾数乘以 \(2^{-2} = 0.25\)。范围：\([1.00, 1.11] \times 0.25 = [0.25, 0.4375]\)。

这四个精确值是：

| 尾数 | 二进制计算 | 十进制 |
|------|------------|--------|
| 00   | 1.00 × 0.25 | **0.25**  |
| 01   | 1.01 × 0.25 | **0.3125** |
| 10   | 1.10 × 0.25 | **0.375**  |
| 11   | 1.11 × 0.25 | **0.4375** |

ULP = 0.0625，刻度非常细密。

试试存数：

- **0.3** → 落在 0.25 和 0.3125 之间，离 0.3125 更近 → 存为 **0.3125**
- **0.35** → 落在 0.3125 和 0.375 正中，平局！IEEE 754 默认“向最近偶数舍入”，看尾数最低位：0.3125（尾数01）末位是1，0.375（尾数10）末位是0，选末位为0的 → **0.375**
- **0.4** → 介于 0.375 和 0.4375 之间，离 0.375 更近 → 存为 **0.375**

---

#### 指数 = -1（存储值 2）

真实指数 -1，乘以 \(2^{-1} = 0.5\)。范围：\([1.00, 1.11] \times 0.5 = [0.5, 0.875]\)。

四个精确值：

| 尾数 | 十进制 |
|------|--------|
| 00   | **0.5**  |
| 01   | **0.625** |
| 10   | **0.75**  |
| 11   | **0.875** |

ULP = 0.125，刻度变粗了些。

试试存数：
- **0.6** → 在 0.5 和 0.625 之间，离 0.625 近 → **0.625**
- **0.7** → 在 0.625 和 0.75 之间，离 0.75 更近 → **0.75**
- **0.8** → 在 0.75 和 0.875 之间，离 0.75 更近 → **0.75**

---

#### 指数 = 0（存储值 3）

真实指数 0，乘以 \(2^0 = 1\)。范围：\([1.00, 1.11] \times 1 = [1.0, 1.75]\)。

四个精确值：

| 尾数 | 十进制 |
|------|--------|
| 00   | **1.0** |
| 01   | **1.25** |
| 10   | **1.5**  |
| 11   | **1.75** |

ULP = 0.25。

试试存数：
- **1.3** → 离 1.25 更近 → **1.25**
- **1.4** → 离 1.5 更近 → **1.5**
- **1.6** → 离 1.5 更近 → **1.5**
- **1.7** → 离 1.75 更近 → **1.75**

---

#### 指数 = 1（存储值 4）

真实指数 1，乘以 \(2^1 = 2\)。范围：\([1.00, 1.11] \times 2 = [2.0, 3.5]\)。

四个精确值：

| 尾数 | 十进制 |
|------|--------|
| 00   | **2.0** |
| 01   | **2.5** |
| 10   | **3.0** |
| 11   | **3.5** |

ULP = 0.5。

试试存数：
- **2.1** → 离 2.0 近 → **2.0**
- **2.7** → 离 2.5 近 → **2.5**
- **3.25** → 恰好在 3.0 和 3.5 正中，平局。3.0（尾数10）末位0，3.5（尾数11）末位1 → 选 **3.0**
- **3.4** → 离 3.5 更近 → **3.5**

---

#### 指数 = 2（存储值 5）

真实指数 2，乘以 \(2^2 = 4\)。范围：\([1.00, 1.11] \times 4 = [4.0, 7.0]\)。

四个精确值：

| 尾数 | 十进制 |
|------|--------|
| 00   | **4.0** |
| 01   | **5.0** |
| 10   | **6.0** |
| 11   | **7.0** |

ULP = 1.0，刻度粗到连整数之间都有洞了。

试试存数：
- **4.3** → 离 4.0 近 → **4.0**
- **5.5** → 正中，平局。5.0（尾数01）末位1，6.0（尾数10）末位0 → 选 **6.0**
- **6.8** → 离 7.0 近 → **7.0**

---

#### 指数 = 3（存储值 6，正规数天花板）

真实指数 3，乘以 \(2^3 = 8\)。范围：\([1.00, 1.11] \times 8 = [8.0, 14.0]\)。

四个精确值：

| 尾数 | 十进制 |
|------|--------|
| 00   | **8.0** |
| 01   | **10.0** |
| 10   | **12.0** |
| 11   | **14.0** |

ULP = 2.0，连9和11这种简单整数都找不着自己的格子了。

试试存数：
- **9** → 刚好在 8.0 和 10.0 正中，平局。8.0（末位0）→ 选 **8.0**
- **11** → 在 10.0 和 12.0 正中，12.0（末位0）→ 选 **12.0**
- **13** → 离 12.0 更近 → **12.0**

---

### 3. 橡皮筋的两端——零、次正规数与无穷

前面我们只逛了正规数的“闹市区”。现在，我们要去探访这根魔法橡皮筋的两个端点——那里藏着 **0**、**无穷大**，还有一片叫 **次正规数** 的神秘过渡带。

回顾一下：3个指数位，存储值`000`和`111`是保留的VIP。

- **指数`000`** → 用来优雅地表示0，并铺出一条通往最小的正规数（0.25）的“地毯”，即次正规数。
- **指数`111`** → 用来表示无穷大和那个“不知道”的NaN。

---

### 指数位全0：0的优雅，与次正规数的“渐变消失”

当指数存储为`000`时，规则全变了：

1.  **隐藏的整数1不见了**，尾数前默认为`0`。
2.  **真实指数固定为 -2**，与最小正规数所在区间的指数保持一致。这使得次正规数区间的ULP与正规数最小的ULP完全相同，实现了丝滑过渡。

我们来看看，这一套新规则带来了什么。

#### 尾数为`00`：绝对的零

按新规则，数值 = `0.00 × 2^(-2)` = 0。这就是我们浮点世界的“原点”。注意，因为符号位还在，所以有 `+0` 和 `-0` 之分。它们大多数时候一样，但在 `1/(+0) = +∞` 和 `1/(-0) = -∞` 时会分道扬镳。

#### 尾数为`01, 10, 11`：次正规数的过渡带

它们仨就是所谓的“次正规数”，填补了0和最小正规数0.25之间的空白。

| 尾数  | 二进制计算       | 十进制        |
| --- | ----------- | ---------- |
| 00  | 0.00 × 0.25 | **0** (零点) |
| 01  | 0.01 × 0.25 | **0.0625** |
| 10  | 0.10 × 0.25 | **0.125**  |
| 11  | 0.11 × 0.25 | **0.1875** |

你看，它们的ULP也是**0.0625**，跟指数-2的正规数区间保持了一致。它们就像一条精美的“坡道”，让数字从0开始，均匀、平滑地过渡到“闹市区”的0.25，避免了0和0.25之间出现一个突兀的数字悬崖。

---

### 指数位全1：无穷大与“不知道”

当指数存储为`111`时，我们来到了数字宇宙的“天花板”和“未知领域”。

#### 尾数为`00`：无穷大（∞）

数值解读为无穷大。同样有**正无穷**和**负无穷**。任何超出我们最大正规数14.0的数字，比如15、100，都会被舍入到正无穷。这就是“溢出”。

#### 尾数非`00`：NaN（Not a Number，不是一个数字）

只要尾数不全是0，无论是`01`、`10`还是`11`，都表示NaN。它是用来记录“计算错误”的，比如 `0/0`、`∞ - ∞`、`√(-1)` 的结果。NaN像个小信号弹，提醒你计算链里某个环节出了逻辑问题，结果已经不可信了。

---

### 4. 把全部数字铺开，规律赫然在目

现在，我们的6比特世界终于完整了。全部32个状态铺满了从0到无穷大的整个数轴。我们把全部正规数并列在一张表里，规律会自己跳出来：

| 指数  | 尾数00 | 尾数01   | 尾数10  | 尾数11   | ULP（间距） |
| --- | ---- | ------ | ----- | ------ | ------- |
| -2  | 0.25 | 0.3125 | 0.375 | 0.4375 | 0.0625  |
| -1  | 0.5  | 0.625  | 0.75  | 0.875  | 0.125   |
| 0   | 1.0  | 1.25   | 1.5   | 1.75   | 0.25    |
| 1   | 2.0  | 2.5    | 3.0   | 3.5    | 0.5     |
| 2   | 4.0  | 5.0    | 6.0   | 7.0    | 1.0     |
| 3   | 8.0  | 10.0   | 12.0  | 14.0   | 2.0     |

### 规律一：相邻指数之间，边界完美接续，呈2倍缩放

把每个指数档的“尾数00”（即该档最小值）拎出来：0.25 → 0.5 → 1.0 → 2.0 → 4.0 → 8.0，每一个都是前一个的2倍。“尾数11”（即该档最大值）同样如此：0.4375 → 0.875 → 1.75 → 3.5 → 7.0 → 14.0。每一档的边界恰好是上一档的两倍，这些区间像俄罗斯套娃一样严丝合缝地接在一起，覆盖了从0.25到14.0的全部范围，中间没有缝隙。

### 规律二：ULP随区间等比翻倍，刻度越来越稀

ULP的公比也是2。区间范围翻倍，刻度间距也翻倍，所以每个区间内始终只有4个点——不多不少。这揭露了浮点数的核心取舍：**精度（刻度密度）与范围（能表示的最大值）是同一枚硬币的两面。** 你想把天花板抬高一倍，整个上半部分的刻度就得粗一倍。

### 规律三：整个数轴上的点是对数均匀的

把全部数字画在数轴上，靠近0的地方密密麻麻挤满了点，越往右空隙越大，最后稀稀拉拉。这恰好是对数尺度的特征：1、2、4、8这些区间的起点在乘法上是等比例（×2），画在数轴上却变成了等间距。浮点数用“换挡”的方式，把乘法上的“等比例”变成了数轴上的“等间隔”。这不均匀，但在乘除法的世界里，恰恰是另一种“公平”。

---

### 5. 这些规律体现了什么？

### 一套优雅的折中方案

IEEE 754用固定数量（尾数宽度）的刻度，通过指数的“换挡”，覆盖了从极小到极大的海量范围。它牺牲了“各处精度一致”，换来了“十亿亿倍范围全覆盖”。

### 对真实世界的自然模拟

现实中，我们也是在小尺度上精细、大尺度上粗糙：量体重的秤，50公斤到51公斤的差距有意义，但50.01公斤就忽略不计；量地球到太阳的距离，小数点后的一切都可以直接舍去。浮点数自动地给了我们这种“按需精度”——不需要你手动切换，它自己就知道该细的地方细，该粗的地方粗。

### 所有浮点误差的根源

理解了这套“对数标尺”，你就能一眼看穿所有经典问题：
- **为什么0.1永远存不精确？** 因为0.1落在两个刻度之间，必须舍入到最近的那个。
- **为什么大数加小数等于白加？** 如果那个小数比大数所在区间的ULP还小，它直接被吞噬，就像往银河里扔一粒沙子。
- **为什么两个接近的浮点数相减会灾难性抵消？** 因为粗刻度上误差的绝对值本来就大，相减后有效数字全丢了。

---

### 6. 总结：讲给12岁孩子的浮点数故事

想象一下，你有一根魔法橡皮筋，上面画了4个刻度点。橡皮筋可以拉长，也可以缩短，但**上面的刻度点永远只有4个**。

- 当橡皮筋很短的时候，刻度挤在一起，你能很精细地量出小东西的长度，比如0.3125。
- 当你把橡皮筋拉得很长很长，刻度之间的间距就变得巨大，这时候你只能大差不差地说：“嗯，大概在8和10之间吧。”连9都标不出来，只能往8或10上靠。
- 更妙的是，这根橡皮筋不会在零点突然断裂。它有一条柔缓的“渐变带”，让自己平缓地融化在零的起点，而不是在细微处戛然而止。
- 当拉到极限，超出所有刻度时，它会明确告诉你“我够不着了（无穷）”；如果让它做没意义的蠢事，它也会直接告诉你“我不知道（NaN）”。

现在再看这根橡皮筋，你会发现一个奇妙的规律：**无论你把它拉到多长，相邻两个刻度之间的距离，永远跟这根橡皮筋的总长度成正比。** 拉长到原来的两倍，刻度间距也跟着变成两倍。所以虽然刻度点只有4个，但它们总能均匀地铺满整根橡皮筋，不会挤在一头，也不会空出中间。

**浮点数，就是这样一根可伸缩的刻度尺。** 它只能同时拥有两种好东西中的一个：要么能量很细的小数字，要么能量很远的大数字，但永远没法在量很大的同时又量得很细。它的数在数轴上不是均匀排队的，而是像一把“对数标尺”——每一档刻度的间距恰好是前一档的两倍。它可以细如发丝地测灰尘，但去量银河时，连地球都只能大约地“贴”在最近的整格子上。

所以记住这个画面：**浮点数就是一根带着4个刻度的魔法橡皮筋，你拉长它，它就变粗糙；你缩短它，它就变精细——而且无论拉多长，刻度总是均匀地铺满全身。它天生懂零，能渐变消失，知边界，会报错。** 这个规律不是Bug，是IEEE 754设计者留下的最美特征。当你知道这个秘密后，写程序时就会格外小心——别让那些被悄悄挪动的小数点，绊倒你的计算结果。